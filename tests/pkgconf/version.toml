[setup]
  [setup.env]
    PKG_CONFIG_PATH="{test_root}/lib1"

[atleast_valid]
  args = ["--atleast-version", "1.0", "foo"]
  expected_fail = true

[atleast_invalid]
  args = ["--atleast-version", "2.0", "foo"]
  exitcode = 1
  expected_fail = true

[exact_valid]
  args = ["--exact-version", "1.2.3", "foo"]
  expected_fail = true

[exact_invalid]
  args = ["--exact-version", "1.0", "foo"]
  exitcode = 1
  expected_fail = true

[max_valid]
  args = ["--max-version", "2.0", "foo"]
  expected_fail = true

[max_invalid]
  args = ["--max-version", "1.0", "foo"]
  exitcode = 1
  expected_fail = true
